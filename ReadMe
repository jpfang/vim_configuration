if you want to use tmux with vim configuration(255color), plz add below command line in your .bashrc, otherwise color will get wrong!
	+ alias tmux="TERM=screen-256color-bce tmux"

Remember
	install ctags, for example on ubuntu "apt-get install ctags"
